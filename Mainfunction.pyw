def main():
    #Read camera
    #Find features
    #find location of the blobs
    #clasify the blobs
    #convert screen to robot coordinates for each blob of interest
    #convert landmarks into robot coordinates
    #compare blobs with landmarks
    #Destinate position baced on landmarks
    #convert to world coordinates
    #add landmarks to list in world coordinates
    #update estimated position for landmarks
    #determine where to drive
    